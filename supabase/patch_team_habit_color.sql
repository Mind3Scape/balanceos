-- Цвет общей (командной) привычки — чтобы полная форма создания/правки привычки в команде
-- (унифицирована с личной, David) реально сохраняла выбранный цвет. Клиент работает и БЕЗ этой
-- колонки (cloud.js вставляет/читает color «слоями» с graceful-fallback) — но пока колонки нет,
-- цвет не переживёт перезагрузку. Запусти ОДИН раз в Supabase → SQL Editor.
alter table public.team_habits
  add column if not exists color text;

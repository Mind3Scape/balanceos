-- ════════════════════════════════════════════════════════════════════════════
--  BalanceOS — КОМАНДНАЯ ЦЕЛЬ считается ИЗ ПРИВЫЧЕК. Запусти ОДИН раз:
--    Supabase → SQL Editor → New query → вставь ВЕСЬ файл → RUN. Безопасно повторно.
--
--  Проблема: у командной цели хранились только goal_kind (по ошибке — ТЕКСТ названия) и
--  goal_target; РЕЖИМ (collective/streak/race), единица и т.п. терялись, а прогресс висел
--  на нуле (хардкод). Идея David: «прогресс считается ИЗ ПРИВЫЧЕК».
--
--  Решение БЕЗ новой таблицы: вклад уже лежит в team_habit_logs (per-user-per-day отметки).
--  Достаточно хранить КОНФИГ цели в одной jsonb-колонке, а прогресс СЧИТАТЬ из team_habit_logs:
--    collective — сумма отметок всех; race — у кого больше (лидер); streak — мин. серия (все держат).
--  Клиент: createTeam пишет teams.goal = {type,target,unit,title}; teamGoalProgress(teamId)
--  читает goal + team_habit_logs и считает current + вклад каждого участника.
-- ════════════════════════════════════════════════════════════════════════════

alter table public.teams add column if not exists goal jsonb;

-- Готово. Новой таблицы/прав не нужно: team_habit_logs (отметки) и его RLS уже на месте
-- (patch_team_approvals.sql) — участники читают отметки команды, прогресс деривится на клиенте.

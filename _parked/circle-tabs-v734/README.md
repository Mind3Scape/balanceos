# Отложено: ЭКРАН КРУГА С ТРЕМЯ ВКЛАДКАМИ (Обзор · Привычки · Чат, v734–v764)

Отложено 2026-07-16 по решению David: «всё старое заархивируй условно, чтобы если что
можно было восстановить и оно не мешалось, и начинай полностью реализовывать новое
с основ, чтобы было как в дизайнах» (макеты И·К·Л финала `_devgoal3.html`).

**Причина.** Комната круга переехала на ОДИН сплошной экран (макет И): нить дня →
серия круга → «Мой день» с чекбоксами → люди → «Пульс дня», в который влит чат.
Вкладки, орбита в шапке, отдельные разделы «Дела»/«Просьбы»/«Люди+календарь»
и большая карточка банка в этот стандарт не входят.

## Что здесь
- `team_detail_tabs.jsx` — ПОЛНЫЙ прежний `TeamDetailLive` (строки 4574–5195
  community_live.jsx на v764): три вкладки, шапка-герой с кольцом заряда, чипы,
  просьбы Э3 (`addTeamRequestCloud`/`claimRequest`), встроенный `TeamChatLive`,
  блок «Люди + календарь», банк XP с выплатами.

## Что осталось В ПРИЛОЖЕНИИ (воскрешение без миграций)
- Вся подводка данных живёт дальше в новой комнате (`screens/live/circle_room_live.jsx`):
  ростер, привычки, отметки, дела, чат, выплаты банка, конфетти — механика не удалялась.
- «Просьбы» как ДАННЫЕ целы: строки `team_tasks.kind='request'` в базе, функции
  `addTeamTask(kind)`/`claimTeamRequest` в cloud.js. Ушёл только UI со старого экрана —
  вернётся переосмысленным в Сообществе v2 (примитив «Просьба»).
- Компоненты `TeamOrbitLive`, `TeamPeopleCalendarLive`, `CirclePeopleCalendarBlockLive`
  остались в community_live.jsx (дремлют, их никто не зовёт) — удалять не стал,
  чтобы воскрешение вкладок было чистым откатом.
- `TeamChatLive` жив и используется отдельным экраном `team-chat`.

## Как воскресить (10 минут)
1. Вернуть содержимое `team_detail_tabs.jsx` в конец `screens/live/community_live.jsx`.
2. Убрать `screens/live/circle_room_live.jsx` из `scripts/build.js` (FILES) и
   `index.html` (script-тег) — иначе два объявления `TeamDetailLive` перетрут друг друга.
3. `node scripts/build.js`, поднять версию. Роут `team-detail` уже указывает на имя.

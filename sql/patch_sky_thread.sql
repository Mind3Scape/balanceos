-- ════════════════════════════════════════════════════════════════════════════
--  BalanceOS — «НЕБО-НИТЬ» (лента дня: кто во сколько отметился). Запусти ОДИН раз:
--    Supabase → SQL Editor → New query → вставь ВЕСЬ файл → RUN. Безопасно повторно.
--
--  Идея (David): чувство «вместе» без чата — лица участников ложатся на полосу
--  неба (рассвет→ночь) в час своей отметки. Время отметок в КРУГАХ уже пишется
--  (team_habit_logs.created_at) — этот патч добавляет то же самое СОВМЕСТНЫМ
--  привычкам (shared_habit_logs), где колонки времени не было.
--
--  Старые отметки останутся с created_at = NULL — приложение честно рисует их
--  без позиции на нити (считает в «N из M», но лицо на небо не ставит).
-- ════════════════════════════════════════════════════════════════════════════

alter table public.shared_habit_logs
  add column if not exists created_at timestamptz default now();

-- Новые вставки штампуются сами (default now()); ничего больше не нужно.
-- RLS не меняем: читать логи могут только участники той же общей привычки,
-- писать — только свои (политики из patch_shared_habits.sql уже это дают).
